# Append `.local` if using secrets
# See https://vite.dev/guide/env-and-mode

# Application Settings
NODE_ENV=development

# Mock Provider Flags (Phases 1-9)
USE_AUTH_MOCKS=true
USE_DB_MOCKS=true

# OAuth Configuration
AUTH_REDIRECT_URI=http://localhost:5173/api/auth/callback