// Configuration object
export const config = {
	// Export individual URLs for explicit access if needed
	urls: {
		production: 'https://tonguetoquill.app',
		staging: 'https://tonguetoquill.vercel.app'
	}
} as const;
